# VMware Workstation + NixOS "一机双网" 网络配置方法

> 目标：虚拟机外网全部走 Windows 主机上的 **TUN-VPN**，同时保持物理局域网互访。

---

## 1 网络拓扑总览

```
┌───────────────┐         192.168.0.0/24         ┌───────────────┐
│   Mac / NAS   │ ────────────────────────────── │  VM ens33     │
└───────────────┘                                 │ 192.168.0.205│
                                                  └────┬─────────┘
                                               LAN     │(桥接)
                     Host-only / VMnet1                ▼
┌───────────────┐ 192.168.137.1/24  ┌─────────────────────────────┐
│  Windows Host │──────────────────▶│  VM ens36 192.168.137.33   │
│ ICS + TUN-VPN │   (ICS / NAT)     └─────────────────────────────┘
└───────────────┘              (默认网关 & DNS)
```

* **ens36 (Host-only / VMnet1)** ：固定 `192.168.137.33/24`，唯一默认网关 `192.168.137.1` → NAT 到 VPN。
* **ens33 (Bridged)** ：DHCP 获取 `192.168.0.x`，显式禁止默认路由（高 Metric），提供局域网访问。

---

## 2 Windows 端操作

| 步骤 | 作用 |
|------|------|
|① 连接 TUN-VPN | 获得加密外网出口 |
|② *ncpa.cpl* → VPN 属性 → 共享 → 选 **VMnet1** | 启用 **ICS**，把 VPN 共享给 VMnet1，自动设置 `192.168.137.1/24` |
|③ VMware *Virtual Network Editor* → **VMnet1** 子网改 `192.168.137.0/24` | 与 ICS 网段统一，避免双重 NAT/DHCP 冲突 |

---

## 3 VMware 虚拟机网卡

| 适配器 | 模式 | 备注 |
|---------|------|------|
| Network Adapter 1 | **Bridged (自动)** | MAC `00:0c:29:65:19:02` |
| Network Adapter 2 | **Host-only (VMnet1)** | MAC `00:0c:29:65:19:0c` |

> 桥接卡优先级较低，可视情况在 `.vmx` 里调 `ethernet0.txbuffers` 等，但非必须。

---

## 4 NixOS（systemd-networkd）配置片段

```nix
networking.useDHCP = false;
networking.nameservers = [ "192.168.137.1" "1.1.1.1" ];

systemd.network.enable = true;

systemd.network.networks."10-ens36" = {
  matchConfig.Name = "ens36";
  networkConfig = {
    Address     = "192.168.137.33/24";
    Gateway     = "192.168.137.1";
    DNS         = "192.168.137.1";
    RouteMetric = 100;   # 主出口
  };
};

systemd.network.networks."20-ens33" = {
  matchConfig.Name = "ens33";
  networkConfig = {
    DHCP = "yes";        # IPv4 DHCP
  };
  dhcpConfig.RouteMetric   = 6000;  # 备份出口，高优先级数 = 低优先级
  linkConfig.RequiredForOnline = "no";
};

systemd.network.wait-online.enable = false;
```

关键点
* `RouteMetric` 必须位于 `dhcpConfig` 中。
* 仅 ens36 提供默认路由；ens33 只在 VPN 掉线时接管。

---

## 5 数据流向示意

| 场景 | 路径 |
|-------|------|
| 外网访问 | `VM → ens36 → 192.168.137.1 (ICS) → VPN-TUN → Internet` |
| 局域网访问 | `LANHost → 192.168.0.205 (ens33) → VM` |
| VPN 中断自动回退 | 内核根据 Metric 切换默认路由到 ens33，再恢复 |

---

## 6 安全与回退

* **私网隔离**：192.168.137.0/24 不暴露在物理 LAN，降低攻击面。
* **双出口 + 高 Metric**：VPN 中断时自动回落到桥接；恢复后再切回。
* **IPv6 禁用**：避免外漏。
* **Windows 防火墙 & ICS**：仍可过滤流量。

---

## 7 验证命令

```bash
ip route                # 只应有 0.0.0.0/0 via 192.168.137.1
ping 192.168.137.1      # 测试网关
curl ifconfig.me        # 公网 IP 应为 VPN IP
ssh root@192.168.0.205  # 局域网可达
```

---

## 8 故障排查

1. **默认路由意外切到 ens33** → 检查 VPN/ICS 是否掉线。
2. **无法获取 192.168.137.* 地址** → 确认 Windows ICS DHCP 正常。
3. **RouteMetric 报 extra fields** → 确认写在 `dhcpConfig` 而不是 `networkConfig`。
4. 使用 `journalctl -u systemd-networkd -f` 观察接口上线/路由变化。

---

> 至此，"VPN 私网出口 + 桥接局域网访问" 已稳定运行，可视需要扩展 WireGuard 内网、端口转发或 VLAN 等功能。 